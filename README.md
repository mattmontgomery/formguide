# Form Guide

This site has moved to [formguide.tools.football](https://formguide.tools.football).

This repository now serves only as a redirector. All requests are 301'd to the
new site via `vercel.json`; specific legacy routes are mapped to their
equivalents on the new site, and everything else falls through to the root.
